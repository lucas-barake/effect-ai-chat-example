diff --git a/dist/unstable/sql/SqlSchema.d.ts b/dist/unstable/sql/SqlSchema.d.ts
index c6fd65dc71147d12a7ea2229bdb0c23244910ea3..017f56f4292f630fd337758f13232d60a4b9b77e 100644
--- a/dist/unstable/sql/SqlSchema.d.ts
+++ b/dist/unstable/sql/SqlSchema.d.ts
@@ -16,7 +16,7 @@ export declare const findAll: <Req extends Schema.Top, Res extends Schema.Top, E
     readonly Request: Req;
     readonly Result: Res;
     readonly execute: (request: Req["Encoded"]) => Effect.Effect<ReadonlyArray<unknown>, E, R>;
-}) => (request: Req["Encoded"]) => Effect.Effect<Array<Res["Type"]>, E | Schema.SchemaError, Req["EncodingServices"] | Res["DecodingServices"] | R>;
+}) => (request: Req["Type"]) => Effect.Effect<Array<Res["Type"]>, E | Schema.SchemaError, Req["EncodingServices"] | Res["DecodingServices"] | R>;
 /**
  * Run a sql query with a request schema and a result schema.
  *
@@ -27,7 +27,7 @@ export declare const findNonEmpty: <Req extends Schema.Top, Res extends Schema.T
     readonly Request: Req;
     readonly Result: Res;
     readonly execute: (request: Req["Encoded"]) => Effect.Effect<ReadonlyArray<unknown>, E, R>;
-}) => (request: Req["Encoded"]) => Effect.Effect<Arr.NonEmptyArray<Res["Type"]>, E | Schema.SchemaError | Cause.NoSuchElementError, Req["EncodingServices"] | Res["DecodingServices"] | R>;
+}) => (request: Req["Type"]) => Effect.Effect<Arr.NonEmptyArray<Res["Type"]>, E | Schema.SchemaError | Cause.NoSuchElementError, Req["EncodingServices"] | Res["DecodingServices"] | R>;
 declare const void_: <Req extends Schema.Top, E, R>(options: {
     readonly Request: Req;
     readonly execute: (request: Req["Encoded"]) => Effect.Effect<unknown, E, R>;
diff --git a/src/unstable/sql/SqlSchema.ts b/src/unstable/sql/SqlSchema.ts
index a84c8f4a0b5d76e5c35b20325eaa50ac167d4446..7f5249eb866120201f57297c0c4cf54ed26594c0 100644
--- a/src/unstable/sql/SqlSchema.ts
+++ b/src/unstable/sql/SqlSchema.ts
@@ -23,7 +23,7 @@ export const findAll = <Req extends Schema.Top, Res extends Schema.Top, E, R>(
   const encodeRequest = Schema.encodeEffect(options.Request)
   const decode = Schema.decodeUnknownEffect(Schema.mutable(Schema.Array(options.Result)))
   return (
-    request: Req["Encoded"]
+    request: Req["Type"]
   ): Effect.Effect<
     Array<Res["Type"]>,
     E | Schema.SchemaError,
@@ -46,7 +46,7 @@ export const findNonEmpty = <Req extends Schema.Top, Res extends Schema.Top, E,
 ) => {
   const find = findAll(options)
   return (
-    request: Req["Encoded"]
+    request: Req["Type"]
   ): Effect.Effect<
     Arr.NonEmptyArray<Res["Type"]>,
     E | Schema.SchemaError | Cause.NoSuchElementError,
